from setuptools import setup, find_packages

setup(
    name="serializer",
    version="1.1.1",
    author="Ermolovich Dima",
    packages=find_packages(),
    scripts=['JsonSerializer/JsonSerializer.py']

)
